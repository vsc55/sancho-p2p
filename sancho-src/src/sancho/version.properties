# Filtered by Maven at build time (see pom.xml <resource> filtering).
# VersionInfo reads this so the displayed version tracks the build, instead of
# the number that was baked into the decompiled 0.9.4-59 source.
version=${project.version}
